{ this.props.items && this.props.items.map((item, i) => {
    return  (
      <div className="form-group">
      <label htmlFor={DC FIELD} >{DC FIELD}: </label>
      <input
        type="text"
        id={DC FIELD}
        value=""
        onChange={this.handleChange}
      /></div>
    )
})}
